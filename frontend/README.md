# Frontend setup

1. Install Node.js 20+.
2. Run `npm install`.
3. Run `npm run dev`.
4. Open http://localhost:5173.

The frontend expects the API at http://localhost:8000/api. Set `VITE_API_URL` to override it.

